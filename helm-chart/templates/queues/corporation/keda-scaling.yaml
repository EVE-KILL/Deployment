apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: queue-corporation
spec:
  scaleTargetRef:
    name: queue-corporation
  pollingInterval: 5
  cooldownPeriod: 30
  minReplicaCount: 1
  maxReplicaCount: 10
  triggers:
  - type: redis
    metadata:
      address: redis.evekill.svc.cluster.local:6379
      listName: evekill:corporation
      listLength: "5"
      activationListLength: "5"
