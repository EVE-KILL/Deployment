apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: queue-evewho
spec:
  scaleTargetRef:
    name: queue-evewho
  minReplicaCount: 0
  maxReplicaCount: 2
  triggers:
  - type: redis
    metadata:
      address: redis.evekill.svc.cluster.local:6379
      listName: evekill:evewho
      listLength: "5"
